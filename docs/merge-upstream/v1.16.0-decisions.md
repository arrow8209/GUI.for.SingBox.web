# v1.16.0 合并冲突决策清单

**合并日期**：2026-04-14
**合并目标**：`5ecaf3de2ec84066218cbff9ff90dbe07a0dac94`（Release v1.16.0 commit）
**合并方式**：`git merge 5ecaf3d --no-commit --no-ff`

## 概览

- 上游 commits 数：16
- 自动合并文件数：~38
- 冲突文件数：5
- 决策分布：保本地 1 / 采上游 0 / 手动融合 4
- 升级中断规则触发：无

## 逐块决策

### 1. `bridge/bridge.go`（modify/delete）

- **区域分类**：② 已删除的 Wails 产物
- **冲突性质**：本地早已删除（Wails 入口），上游修改了它
- **决策**：**拒绝上游变更**，`git rm -f bridge/bridge.go`
- **理由**：本 fork 已彻底脱离 Wails，`bridge/bridge.go`（Wails App Bind 入口）不复存在

### 2. `go.mod`

- **区域分类**：⑥ 依赖与构建
- **冲突性质**：上游引入 `wailsapp/wails`、`energye/systray`、`gen2brain/beeep`、`labstack/echo` 等 Wails 系列依赖；本地用 `chi`、`gorilla/websocket`、`go-chi/cors`
- **决策**：**手动融合** —— 拒绝所有 Wails 系列；保留本地依赖；版本号取上游较高（`golang.org/x/sys` 0.38→0.39, `golang.org/x/text` 0.31→0.32）
- **后续操作**：`go mod tidy` 自动把 `go-chi/cors` 从 indirect 升为 direct（main.go 直接使用），符合预期

### 3. `go.sum`

- **区域分类**：⑥ 依赖与构建
- **决策**：`git checkout --ours go.sum` + `go mod tidy` 重生成
- **理由**：sum 是 mod 的派生产物，重生成最干净

### 4. `frontend/src/stores/kernelApi.ts`（双方修改）

- **区域分类**：④ 红区（本地有日志/fallback 增强）
- **冲突 1（runtimeProfile 加载逻辑，行 115-141）**：
  - 本地：try/catch + summarizeInbound 详细日志 + fallback 创建 + 显式 unavailable 早返回
  - 上游：删除了"如果当前 inbound 不存在 runtime 中则 push enable=false"的修复
- **决策**：**手动融合**
  - 保留本地 try/catch + 日志 + fallback 早返回
  - 采纳上游 inbound forEach 处理（修了 enable=false 推入逻辑的 bug）
  - profile 选择改为 `getProfileById(...) || profilesStore.currentProfile`（兼容两套定位策略）
- **冲突 2（mixed/http/socks 查找，行 163-176）**：
  - 本地：仅按 `v.mixed` 查找
  - 上游：`v.enable && v.mixed` 加入 enable 检查
- **决策**：**采纳上游** —— enable 检查是 bug fix，必须采纳；fallback warning 已在外层

### 5. `frontend/src/stores/subscribes.ts`（双方修改）

- **区域分类**：④ 红区（本地有 TLS 错误友好提示功能 a76614f）
- **冲突（updateSubscribe catch 块，行 206-214）**：
  - 本地：`formatSubscribeError(error)` 调用（生成可读的 TLS 错误消息）
  - 上游：`Failed to update subscription [${s.name}]. Reason: ${error.message || error}`（带订阅名前缀）
- **决策**：**手动融合**
  - 保留本地 `formatSubscribeError`（自定义功能必保）
  - 采纳上游"带订阅名前缀"格式
  - 最终：`throw \`Failed to update subscription [${s.name}]. Reason: ${message}\``

## Bridge 按需落地

v1.16.0 上游无新增 `bridge/*.go` 方法（pre-scan 已确认）。无需落地。

## 后续 lint 修正

合并后 `pnpm lint` 报告 2 处 unused import（合并引入的）：

1. `frontend/src/utils/others.ts:4` —— `APP_VERSION` 未使用，删除
2. `frontend/src/App.vue:7` —— `exitApp`、`sampleID` 未使用，删除

均为合并产生的死代码清理，与功能无关。

## 决策清单分类汇总

| 文件 | 区域 | 决策 |
|------|------|------|
| `bridge/bridge.go` | ② | 拒绝上游（删除） |
| `go.mod` | ⑥ | 手动融合（拒 Wails，升版本） |
| `go.sum` | ⑥ | 重生成 |
| `frontend/src/stores/kernelApi.ts` | ④ | 手动融合（保本地日志+fallback，采上游 enable 修复） |
| `frontend/src/stores/subscribes.ts` | ④ | 手动融合（保本地 formatSubscribeError，采上游名称前缀） |
| `frontend/src/utils/others.ts` | ④ | lint fix（删 unused） |
| `frontend/src/App.vue` | ④ | lint fix（删 unused） |

## 备注

- 自动合并的 38 个文件无需逐一审查（git 已成功 3-way merge）；如后续验证发现问题再回溯。
- v1.16.0 总体复杂度低于 pre-scan 估计（实际 5 冲突，pre-scan 估"高风险"是从 commit 性质而非冲突数量考虑）。
