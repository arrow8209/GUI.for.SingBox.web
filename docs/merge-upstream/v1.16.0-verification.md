# v1.16.0 验证记录

**合并日期**：2026-04-14
**验证环境**：Linux 6.8.0-107-generic / 自动化部分 = Claude inline / UI 部分 = 待用户验证

## 编译闸

| 检查 | 状态 |
|------|------|
| `go build ./...`（root pkg） | ✓ PASS（输出 14754470 bytes） |
| `cd frontend && pnpm install` | ✓ PASS（仅 less 构建脚本被忽略，无功能影响） |
| `cd frontend && pnpm build` | ✓ PASS（产出 dist/index.html + dist/assets/*）|
| `pnpm lint`（lint:oxlint + lint:eslint） | ✓ PASS（修复 2 处 unused import 后 0 error / 0 warning） |

非阻塞 warning：
- lightningcss 对 `:deep` pseudo-class 的命名建议（CSS scoped 选择器）—— 历史遗留
- vue-i18n 对 `Vue[name]` 表达式的导出警告 —— vue-i18n 内部，与本次合并无关

## 冒烟闸（自动化）

| 检查 | 状态 |
|------|------|
| 服务启动 :22397/:22398/:22399（PORT 环境变量） | ✓ PASS |
| `/api/login` (admin/admin123) → 200 + token | ✓ PASS |
| `/api/login` (错误密码) → 401 | ✓ PASS |
| 未授权访问 `/api/env` → 401 | ✓ PASS |
| 授权后 `/api/env` → 200 | ✓ PASS |
| `/api/logout` → 200 | ✓ PASS |
| 注销后 `/api/env` → 401 | ✓ PASS |
| `/` 静态资源 → 200 | ✓ PASS |

**冒烟闸 PENDING_USER**（需浏览器人工验证）：
- 浏览器逐页打开（首页、订阅、配置、规则集、插件、日志、设置）无控制台报错
- 内核启停（下载/启动/停止/查看 PID）

## 自定义功能回归闸（12 项）

| # | 项 | 自动化结果 | 待用户验证 |
|---|---|------------|------------|
| 1 | 单管理员鉴权（首启生成 auth.yaml + login/logout/401） | ✓ PASS | 仅"过期 token forceLogout 触发"需 UI 验证 |
| 2 | WebSocket 鉴权（`/ws?token=xxx` 101，无 token 401） | ✓ PASS | — |
| 3 | Core Proxy `/api/core/*` | — | ⏸ 需运行 sing-box 内核 + 前端发起代理请求 |
| 4 | Reality 公钥生成 `/api/reality/public-key` | ✓ PASS（私钥→公钥转换正确） | — |
| 5 | VLESS Reality 入站 UI 编辑 + 持久化 | — | ⏸ 需打开 InboundsConfig 面板 |
| 6 | Trojan TLS 入站 UI 编辑 + 持久化 | — | ⏸ 同上 |
| 7 | 自定义入站 JSON 编辑器（CodeMirror） | — | ⏸ |
| 8 | Shadowsocks 密码生成（按钮点击） | — | ⏸ |
| 9 | 下载代理（GeneralSettings.downloadProxy） | — | ⏸ |
| 10 | 订阅 TLS 错误友好提示 | 代码已确认保留 `formatSubscribeError` | ⏸ 需故意用 https 错证书订阅触发 |
| 11 | 构建 git hash 显示 | — | ⏸ |
| 12 | Exit API `/api/exit` 优雅退出 | ✓ PASS（HTTP 200 + 进程退出） | — |

**自动化通过**：1 / 2 / 4 / 12（4 项）
**待用户验证**：3 / 5 / 6 / 7 / 8 / 9 / 10 / 11（8 项）

## 决策清单 lint 后追加修正

| 文件 | 修正 |
|------|------|
| `frontend/src/utils/others.ts:4` | 删除 unused import `APP_VERSION` |
| `frontend/src/App.vue:7` | 删除 unused imports `exitApp`、`sampleID` |

## TODO（非阻塞瑕疵）

- 内核运行下的 Core Proxy 流量验证（若用户手头有 sing-box 二进制，建议合回 main 前补一次手动）
- 待用户做完 8 项 UI 回归后回填本文档

## 结论

**自动化部分：放行**

- 编译闸全绿
- 冒烟闸（自动可测项）全绿
- 回归闸 12 项中 4 项自动通过；8 项待用户 UI 验证

待用户在浏览器中完成 8 项回归确认后，本版可正式判定放行进 v1.17.0。
