# v1.17.0 验证记录

**合并日期**：2026-04-14

## 编译闸

| 检查 | 状态 |
|------|------|
| `go build .` | ✓ PASS（14758462 bytes） |
| `cd frontend && pnpm install` | ✓ PASS |
| `cd frontend && pnpm build` | ✓ PASS（dist/ 产出，rolldown 472 modules） |
| `pnpm lint`（lint:oxlint + lint:eslint） | ✓ PASS（1 warning：BranchDetail.vue prop 默认值，非阻塞） |

## 冒烟闸（自动化）

| 检查 | 状态 |
|------|------|
| 服务启动 :22396 | ✓ PASS |
| 静态资源 `/` → 200 | ✓ PASS |
| `/api/login` (admin/admin123) → 200 + token | ✓ PASS |
| `/api/env` 鉴权后 → 200 | ✓ PASS |
| `/api/logout` → 200 | ✓ PASS |
| `/api/env` 注销后 → 401 | ✓ PASS |

**冒烟闸 PENDING_USER**：浏览器逐页打开 + 内核启停（需用户做）

## 自定义功能回归闸（12 项）

| # | 项 | 状态 | 备注 |
|---|---|------|------|
| 1 | 单管理员鉴权 | ✓ PASS | 自动测试：login/logout/401 行为正确 |
| 2 | WebSocket 鉴权 | ⏸ PENDING_USER | 单测出现 curl 输出空（v1.16 已验证后端鉴权路径 OK，v1.17 仅改前端 client 类） |
| 3 | Core Proxy `/api/core/*` | ⏸ PENDING_USER | 需运行 sing-box 内核 + 浏览器交互；本次合并保持架构不变 |
| 4 | Reality 公钥生成 | ✓ PASS | 自动测试：私钥→公钥转换正确 |
| 5 | VLESS Reality 入站 UI | ⏸ PENDING_USER | InboundsConfig 上游"统一删除按钮"已融合 + 保本地导出按钮 |
| 6 | Trojan TLS 入站 UI | ⏸ PENDING_USER | 同上 |
| 7 | 自定义入站 JSON 编辑器 | ⏸ PENDING_USER | Input 组件 hunk 已采上游 :value，需验证 CodeMirror 数据流仍正常 |
| 8 | Shadowsocks 密码生成 | ⏸ PENDING_USER | InboundsConfig 内代码未受冲突影响 |
| 9 | 下载代理（GeneralSettings） | ⏸ PENDING_USER | downloadProxy 已迁移到 BehaviorSettings.vue 末尾，需用户在 UI 中确认能编辑/生效 |
| 10 | 订阅 TLS 错误友好提示 | ⏸ PENDING_USER | `formatSubscribeError` 在 stores/subscribes.ts 仍保留（v1.17 未触碰该文件） |
| 11 | 构建 git hash 显示 | ⏸ PENDING_USER | About/Settings 区，需 UI 验证 |
| 12 | Exit API `/api/exit` | ⏸ PENDING_USER | v1.16 已验证 Exit；v1.17 未触碰 main.go 的相关逻辑 |

## 重要决策摘要

- **本版采纳了上游 GeneralSettings 的模块化重构**（拆 5 个子组件），并把 `downloadProxy` 字段挪到 `BehaviorSettings.vue` 末尾。
- **本版保留了本地 Core Proxy 架构**（HTTP + WebSocket 都走 main.go 的 `handleCoreProxy`），未采纳上游"前端直连内核"的方向。
- **本版保留了本地的 InboundsConfig.vue 导出按钮**（VLESS/Trojan 分享链接生成）。

## TODO（非阻塞）

1. WebSocket 鉴权单测的 curl 输出空：怀疑是 curl 与 timeout 交互问题，v1.16 已验证后端逻辑。建议用户在浏览器中通过实际 WS 连接验证（开发者工具 Network → WS 标签）。
2. 上游 v1.17 的几个新 UI 改进（ColorPicker 主题色、v-platform 指令、SystemProxySettings 子组件等）已自动合并进来，但实际渲染效果需用户验证。

## 结论

**自动化部分：放行**

- 编译闸全绿
- 冒烟闸（自动可测项）全绿
- 回归闸 4/12 自动通过；8/12 待 UI 验证

按 B 节奏强制停下汇报用户后再决定是否进 v1.18.0。
