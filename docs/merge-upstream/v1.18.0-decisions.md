# v1.18.0 合并冲突决策清单

**合并日期**：2026-04-14
**合并目标**：`f7d5043fc24e6ec6503285d1c3860c2c20c84be9`（Release v1.18.0 commit）

## 概览

- 上游 commits 数：3
- 冲突文件数：4（2 modify/delete + 2 双方修改）
- 升级中断规则触发：无

## 逐块决策

### 1. `bridge/bridge.go`、`frontend/src/bridge/wailsjs/go/models.ts` (②)

- **决策**：`git rm -f` 删除

### 2. `frontend/src/bridge/io.ts` (① 桥接层)

- 上游 v1.18 加 `Range?: string` 选项支持 byte-range 读写（`a782294 feat(IO): support partial file read/write`）
- 上游 WriteFile/ReadFile 用 `App.WriteFile/ReadFile` 直连 Wails；本地用 httpClient
- **决策**：保本地 httpClient 实现 + 把 `range` 字段透传给后端（即使后端目前不处理也兼容）
- IOOptions 已自动合并加 `Range`

### 3. `frontend/src/bridge/server.ts` (① 桥接层)

- 上游 v1.18 给 StartServer 加 StaticHeaders/UploadHeaders/MaxUploadSize 等新选项（`dc0539e feat(server)`）
- 本地 server.ts 是完整 stub（throw "not supported in web"）
- **决策**：`git checkout --ours`（保 stub）；上游的 ServerOptions 接口/HttpServerHandler 类型未引入（前端无 view 调用 StartServer，按"按需落地"跳过）

### 4. `bridge/server.go`、`bridge/types.go`（自动合并）

- 上游为 main.go server 加新字段；本地 bridge 已 stub，自动合并接受上游新字段
- 无业务影响

## Bridge 按需落地

- 无新增前端调用的 bridge 方法
