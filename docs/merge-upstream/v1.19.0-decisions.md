# v1.19.0 合并冲突决策清单

**合并日期**：2026-04-14
**合并目标**：`d6285e0225ce3672b7e936a3c7a696275189705e`（Release v1.19.0 commit）

## 概览

- 上游 commits 数：4
- 冲突文件数：2（modify/delete，全 Wails）+ 自动合并多文件
- 升级中断规则触发：无

## 决策

### 1. `bridge/bridge.go`、`frontend/src/bridge/wailsjs/go/models.ts` (②)

- 删除

### 2. 自动合并的关键改动

- **`2ee23ea feat: add privileged mode detection and execution support`**：
  - bridge/exec.go 加 `RunWithPowerShell` 等
  - bridge/types.go 加 `isPrivileged: bool` 字段到 EnvData
  - BehaviorSettings.vue 加 admin 重启按钮
- **`9b65d13 fix: update class bindings for Tag component styles`**：自动合并
- **`655fd5b refactor: icon component`**：自动合并

### 3. 类型修正（合并后）

- `frontend/src/bridge/app.ts`：EnvResult 加 `isPrivileged?: boolean`
- `frontend/src/stores/env.ts`：setupEnv 默认 `isPrivileged: false`（GetEnv 未返回时回退）

理由：上游加了 `isPrivileged` 是必填字段；本地 `bridge/app.go` 的 `GetEnv()` 暂未返回该字段，需要可选 + 默认值兼容。

## TODO

- 后端 `bridge/app.go GetEnv()` 未返回 `isPrivileged` —— Linux 下 `geteuid() == 0` 检测可选实现。当前默认 false 即可。
