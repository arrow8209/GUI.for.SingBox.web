# v1.20.0 验证记录

| 闸 | 状态 |
|----|------|
| go build | ✓ PASS |
| pnpm install (新 @tsconfig/node24) | ✓ PASS |
| pnpm build | ✓ PASS |
| pnpm lint (oxlint + eslint) | ✓ PASS（0 warnings, 0 errors） |
| 服务启动 :22503 | ✓ PASS |
| /api/login | ✓ PASS |
| /api/env 鉴权 | ✓ PASS（200） |
| /api/reality/public-key | ✓ PASS |
| /api/logout | ✓ PASS |

## 自动化通过的回归项

1. 单管理员鉴权 ✓
2. （WS 鉴权未单测，逻辑同 v1.17 / v1.19）
3. Core Proxy ⏸（HTTP 走 `/api/core/*` 路径未变；WS 现走相同 Core Proxy + WS upgrade）
4. Reality 公钥 ✓
12. Exit API（沿用本地 `/api/exit`）

## 本版重大变更需 UI 重点验证

1. **WebSocket 内核数据流**（log/memory/traffic/connections）：架构从 `stores/kernelApi.ts` 内部实现搬到 `api/kernel.ts` 全局函数，前端 `kernelApiStore.onLogs/onMemory/...` API 不变但底层走 `wsChannels`
2. **首页 OverView**：用 onMemory/onTraffic/onConnections 显示实时数据
3. **日志页 LogsController**：用 onLogs 显示实时日志
4. **下载代理（BehaviorSettings.vue）**：v1.17 已迁入，本版未触碰
5. **登录页 LoginView**：路由配置 (router/routes.ts) 保本地
6. 通用 UI 组件搬到 `_common/`（无功能改动，但需确认渲染正常）

## 结论

**自动化部分：放行**

按 B 节奏强制停下汇报后再决定是否进 v1.21.0。
