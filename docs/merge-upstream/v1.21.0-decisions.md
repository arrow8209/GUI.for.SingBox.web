# v1.21.0 合并冲突决策清单

**合并日期**：2026-04-14
**合并目标**：`5d61206`（Release v1.21.0）

## 概览

- 上游 commits 数：9
- 冲突文件数：3（2 ②删 + 1 双方修改）
- 升级中断规则触发：无

## 决策

### 1. `bridge/bridge.go`、`frontend/src/bridge/wailsjs/go/models.ts` (②)

- 删除

### 2. `frontend/src/bridge/exec.ts` (① 桥接层)

- 上游加 `WorkingDirectory` 选项（`919a50c feat: add WorkingDirectory option to ExecOptions`）
- **决策**：融合进 mergeExecOptions，保本地 arrow function + assertFlag 结构

### 3. 自动合并的关键改动

- `c1be4cf` / `7a57e59` **normalizeBase64 / isValidBase64 relax**：`utils/others.ts` 加 `normalizeBase64`、`base64UrlEncode`、`base64Decode` 使用新的 base64 宽松解析 —— 订阅解析增强
- `8fc0a40` **latency testing custom timeout**：`api/kernel.ts` 的 `getProxyDelay` 签名加 `timeout: number` 参数
- `f6c8f36` **clipboard pasting + Input validation**：`Input/index.vue` 加 `allowPaste` prop + `ClipboardGetText` 调用
- `57dd601` generator 清理 `RuleActionReject`
- `bridge/exec.go` 自动合并 `cmd.Dir = options.WorkingDirectory`

### 4. 补上游新依赖：`ClipboardGetText`

- 上游 Input 组件调用 `ClipboardGetText` from `@/bridge`，本地 `browser.ts` 只有 `ClipboardSetText`
- **决策**：在 `browser.ts` 新增 `ClipboardGetText` Web 实现（基于 `navigator.clipboard.readText()`，失败返回空串）

## Bridge 按需落地

- `ClipboardGetText`：Web 端 navigator API 实现（无需后端）

## 结论

编译闸 / 冒烟闸全 PASS。
