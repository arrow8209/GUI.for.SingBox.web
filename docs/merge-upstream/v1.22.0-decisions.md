# v1.22.0 合并冲突决策清单

**合并日期**：2026-04-14
**合并目标**：`v1.22.0` tag

## 概览

- 上游 commits 数：18
- 冲突文件数：~15
- **pre-scan 预警项**：manual profile editor（0714535）、Wails native notifications、macOS auto-start

## 关键决策

### 1. Manual profile editor（pre-scan 预警）

- 上游 `0714535 feat: add manual profile editor` 引入 `ProfileEditor.vue` + 修改 `restoreProfile` 签名为 `(raw, name?, options?)`
- 本地 `restorer.ts` 保留自定义 inbounds 还原（VLESS Reality/Trojan 支持）
- **决策**：
  - `restorer.ts` 保本地（`git checkout --ours`），签名不变（只接受 raw）
  - `ProfileEditor.vue` 修改调用方式：只传 `JSON.parse(profileText.value)` 第 1 个参数
- **妥协**：丢失上游 manual editor 的"基于现有 profile 及其订阅 ID 合并编辑"能力；但本地业务场景（直接编辑 profile JSON）仍可用
- 未触发升级中断规则 5（未与 Reality/Trojan 代码路径直接交叉）

### 2. Wails native notifications（pre-scan 预警）

- 上游 `1cb1bd0` 删除 `frontend/src/bridge/notification.ts`，改用 Wails runtime 的 `IsNotificationAvailable/RequestNotificationAuthorization/SendNotification`
- `frontend/src/bridge/app.ts` 加了 Notify 函数调用这些 API
- `ScheduledTaskForm.vue` 也引用它们
- **决策**：
  - 本地 `notification.ts` 保留（modify/delete 冲突选保留，基于浏览器 Notification API）
  - 删除 app.ts 中上游残留的 Notify 函数（本地 notification.ts 已有 Notify 导出）
  - 在 `notification.ts` 加 Web 实现的 `IsNotificationAvailable`、`RequestNotificationAuthorization`、`SendNotification`（调用本地 Notify）
  - `bridge/notification.go` 自动合并的签名加 `NotifyOptions`，`bridge/types.go` 补 `NotifyOptions` struct

### 3. 5 个 modify/delete (②)

- `bridge/bridge.go`、`wailsjs/go/bridge/App.d.ts`、`wailsjs/go/bridge/App.js`、`wailsjs/go/models.ts`、`wailsjs/runtime/runtime.d.ts`、`wailsjs/runtime/runtime.js`
- **决策**：`git rm -f` 删除

### 4. main.go

- 上游残段：Wails 入口 + `runtime.InitializeNotifications`
- **决策**：删除残段，保本地 `AuthConfig` struct

### 5. `frontend/src/stores/env.ts`

- 上游用 `OS.Windows` 枚举（v1.22 引入）替代字符串 `'windows'`
- 本地已加 `isPrivileged: _env.isPrivileged ?? false`
- **决策**：融合 —— 采纳上游 `OS.Windows` + 保本地 `isPrivileged`

### 6. `frontend/src/bridge/app.ts`

- 上游改 GetEnv 签名为 `GetEnv<T>(key?: T)` + 引入 Wails 原生通知 imports
- 本地保 HTTP API 调用
- **决策**：全部保本地（拒绝上游 Wails 直连 + 删除残留 Notify 块）

### 7. `frontend/src/bridge/index.ts`

- 上游从 index 里去掉 `notification/events/window/browser` 导出
- 本地需要这些导出（Web 实现文件还在）
- **决策**：保本地（全部导出）

### 8. `frontend/src/constant/profile.ts`

- 上游改 `DefaultInboundTun` 签名为 `NonNullable<IInbound['tun']>`
- 本地有 `DefaultInboundShadowsocks/Vless/Trojan` 函数
- **决策**：保本地的 Shadowsocks/Vless/Trojan + 所有 Default 函数都改用 `NonNullable<...>` 签名

### 9. `frontend/src/router/routes.ts`

- 上游删除 `import LoginView`
- 本地需要 LoginView
- **决策**：保本地 import

### 10. `frontend/tsconfig.app.json`

- 上游加 `@wails/*` 路径别名
- 本地不需要 Wails 路径
- **决策**：拒绝 `@wails/*` 别名，采纳 `tsBuildInfoFile` 配置

### 11. `frontend/src/utils/tray.ts`

- 上游引入大量 tray 实现代码（Wails 相关）
- **决策**：`git checkout --ours` 保本地 stub

### 12. `go.mod` / `go.sum`

- 上游升级 Wails 到 2.12 并加 samber/lo 等依赖
- **决策**：`git checkout --ours` + `go mod tidy`

### 13. 自动合并的 BehaviorSettings.vue

- 上游引入 `EnableAutoStart/DisableAutoStart/IsAutoStartEnabled` 全平台 auto-start
- 本地 downloadProxy 字段在 v1.17 已加到该文件，自动合并保留了
- 未触发冲突

### 14. 后续清理

- `frontend/src/utils/others.ts`: 删除 `useEnvStore` unused import（lint）

## Bridge 按需落地

| 上游新增 bridge | 本地实现 |
|----------------|---------|
| `IsNotificationAvailable` | `notification.ts` 加 Web 版（`'Notification' in window`） |
| `RequestNotificationAuthorization` | Web `Notification.requestPermission` |
| `SendNotification` | 委托给本地 `Notify` |

## 结论

编译闸 / lint / smoke 全 PASS。
