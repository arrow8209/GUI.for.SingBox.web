# v1.22.0 验证记录

| 闸 | 状态 |
|----|------|
| go build | ✓ PASS |
| pnpm build | ✓ PASS |
| pnpm lint | ✓ PASS（0 warnings / 0 errors） |
| 服务启动 :22505 | ✓ PASS |
| /api/login | ✓ PASS |
| /api/env 鉴权 | ✓ PASS |
| /api/reality/public-key | ✓ PASS |

## 本版关键变更需 UI 验证

1. **Manual profile editor**：ProfileEditor.vue 改为只传 1 参数；上游的"基于现有 profile 合并编辑"能力未移植，需用户确认该场景在 UI 中是否用到
2. **Notification API 迁移**：新增 `IsNotificationAvailable/RequestNotificationAuthorization/SendNotification` 的 Web 实现。需 ScheduledTaskForm 等页面验证
3. **Outbound icon/hidden 属性**：自动合并纳入，需 UI 验证
4. **subscription-aware outbounds in profile restore**：自动合并纳入

**结论：放行**
