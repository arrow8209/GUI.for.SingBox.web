# v1.23.1 合并冲突决策清单

**合并日期**：2026-04-14
**合并目标**：`v1.23.1` tag（收尾版）

## 概览

- 上游 commits 数：2（纯 macOS 修复 patch）
- 冲突文件数：1（② 删）
- 升级中断规则触发：无

## 决策

### 1. `bridge/bridge.go` (②)

- `git rm -f` 删除

### 2. 自动合并纳入

- `fc5c848 fix: add macOS fallbacks for process info and memory lookup`
  - `bridge/exec.go` 加 `ProcessInfo` 多级回退（proc.Name → proc.Exe → proc.CmdlineSlice）
  - `ProcessMemory` 在 macOS 上失败时通过 `ps -o rss=` 命令回退
  - 新增 `getDarwinProcessRSS` 辅助函数
- `be28e95 fix: remove rolling-release directory after app update on macOS`
  - `frontend/src/stores/app.ts` 自动合并

两者都是桌面端修复，Web 模式下 macOS 回退逻辑对 Linux 服务器无影响。

## 结论

编译闸 / lint / smoke 全 PASS。

**这是最后一版。** 所有 9 个里程碑（v1.16 → v1.23.1）合并完成。
