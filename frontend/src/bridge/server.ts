export const StartServer = async () => {
  throw new Error('Custom HTTP servers are not supported in the web version.')
}

export const StopServer = async () => {
  throw new Error('Custom HTTP servers are not supported in the web version.')
}

export const ListServer = async () => {
  return []
}
